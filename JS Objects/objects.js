let user = {
    name: 'Paul',
    age: 39,
    location: 'Toronto'
}
console.log(`${user.name} is ${user.age} and lives in ${user.location}`)

user.age ++

console.log(`${user.name} is ${user.age} and lives in ${user.location}`)